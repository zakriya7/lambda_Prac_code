
version: 0.2
phases:
  install:
    commands:
      - sam package --template-file sam-template.yaml --s3-bucket zak-test-bucket --output-template-file outputSamTemplate.yaml                              
artifacts:
  type: zip
  files:
    - sam-template.yaml
    - outputSamTemplate.yaml
   
